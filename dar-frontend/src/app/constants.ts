export const DELAY = 1500;
export const MARGIN = { top: 30, right: 30, bottom: 30, left: 30 };
export const BAR_PADDING = 0.3;
export const DATE_FORMAT = "DD.MM.YYYY";
//graphs
export const GRAPH_MARGIN = 40;

export const WIDTH = 460 - MARGIN.left - MARGIN.right;
export const HEIGHT = 400 - MARGIN.top - MARGIN.bottom;
export const RADIUS = Math.min(WIDTH, HEIGHT) / 2 - GRAPH_MARGIN;
